-- INSERT into restaurant(
--     name, description, number_clients
-- )
-- value('Da MySQL', 'ERROR_AUTH_MODE', 22);
-- DELETE from reastaurant where id = 1;

-- UPDATE menu SET restaurant_id = 1;
-- INSERT into menu( dish_name, ingredients, price)
-- value('carnazza', 'carne e altra carne', 20);

-- UPDATE menu SET restaurant_id = 1;

-- INSERT into clients(client_name, client_cuenta)
-- value('Frankino', 22);

-- INSERT into menu_clients(menu_id, clients_id)
-- value(1, 1);


-- INSERT into menu_clients(menu_id, clients_id)
-- value(1, 2);


-- INSERT into menu_clients(menu_id, clients_id)
-- value(2, 3);

INSERT into menu(dish_name, price)
values('insalatina', 35);