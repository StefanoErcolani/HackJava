import java.util.Arrays;
public class App {
    public static void main(String[] args) throws Exception {
        // String[] names = new String[]{"Ciao", "chi", "sono", "io"};
        // Arrays.sort(names, (a,b) -> a.length() - b.length());
        // System.out.println(String.join("@", names));
    
    }
}
